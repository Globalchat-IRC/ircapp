{
  "name": "globalchat-gateway",
  "version": "1.0.0",
  "description": "WebSocket-to-TCP IRC gateway for GlobalChat web client",
  "main": "websocket-gateway.js",
  "scripts": {
    "start": "node websocket-gateway.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "puppeteer": "^21.0.0",
    "ws": "^8.19.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
